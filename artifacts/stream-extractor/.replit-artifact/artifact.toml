kind = "web"
previewPath = "/"
title = "Stream Link Extractor"
version = "1.0.0"
id = "artifacts/stream-extractor"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21395

[services.development]
run = "pnpm --filter @workspace/stream-extractor run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/stream-extractor", "run", "build" ]
publicDir = "artifacts/stream-extractor/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21395"
BASE_PATH = "/"
